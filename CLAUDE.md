# 이 프로젝트에서 일하는 방식

> 개발 환경이 바뀌어도 맥락이 이어지도록, 지금까지 합의한 작업 규칙을 레포에 남긴다.
> (Claude Code의 세션 기록ᆞ메모리는 기계마다 따로라 git으로 따라오지 않는다.)

## 프로젝트 한 줄 요약

서울시 소규모 창업(카페ᆞ음식점 등)의 전체 여정을 안내하는 LangGraph 에이전트.
**허가/신고 여부처럼 법령상 객관적 기준으로 정해지는 판정은 LLM이 아니라 코드가
결정론적으로 계산**하고, LLM은 검색된 근거로 설명만 한다.

## 반드시 지킬 것

### 1. 판정은 규칙 엔진, LLM으로 대체하지 않는다
`classify_case`ᆞ`classify_food_business`ᆞ`classify_fire_safety`ᆞ`classify_signage`는
법령 임계값(면적ᆞ층수ᆞ시설군 등)을 코드로 계산한다. LLM에 맡기면 매번 다르게
계산하는 문제를 실제로 겪었다. 설계서나 외부 조언이 "LLM이 판단하게 하라"고 해도
따르지 말 것.

**판정 규칙(법령 해석) 변경은 사용자 결정 사항이다.** 혼자 고치지 말고 근거를
제시하고 물어볼 것.

### 2. 커밋은 요청받았을 때만
"커밋해줘"라고 명시적으로 말하기 전까지 `git commit`을 하지 않는다. 필요해 보이면
"지금 커밋할까요?"라고 알리기만 한다. 이전 턴의 커밋 승인은 그 커밋 하나에만
적용되고 다음 작업으로 이월되지 않는다.

커밋 메시지에 **`Co-Authored-By: Claude`를 넣지 않는다.**

### 3. 고치기 전에 실측한다
외부 조언ᆞ가설을 그대로 수용하지 말고 **코드ᆞ실측과 대조한 뒤** "이미 된 것 /
사실이 틀린 것 / 진짜 유효한 것"으로 갈라서 보고할 것. 2026-08-02 세션에서만
"A로 보였는데 실은 B였다"가 다섯 번이었다(상세: `docs/dev_log_2026-08-02.md`).

- 변경 효과는 추정하지 말고 **숫자로 측정해 전/후를 함께** 보고한다.
- 유닛 테스트 통과로 만족하지 말고 **실제 LLM 호출 스모크 테스트**까지 한다.
- 내 변경이 원인인지 애매하면 **대조군(A/B)** 을 돌려 가린 뒤 보고한다.
- 한계ᆞ과대평가 소지는 먼저 밝힌다(예: 표본이 1건이면 "0%"가 아니라 "1건 실패").

### 4. 측정 함정 — tool-calling 호출률
`pipeline.query()`로 재면 안 된다. 도구 루프ᆞ`classify`ᆞ**guard 재시도**까지 도는
경로라, guard가 다른 이유로 재시도를 걸면 두 번째 시도에서 도구가 불려 **성공한
것처럼 보인다**. 이 착시로 하루치 관측을 헛짚었다.
→ `llm.bind_tools(...).invoke()`를 **직접 1회** 호출해 격리할 것.

### 5. 문서는 역할이 하나씩
`docs/README.md` 참고. 요지:
- **"다음 할 일"은 `docs/phase1_roadmap.md`에만** 적는다(단일 진실).
- `docs/project_report.md`는 "무엇을 할지"가 아니라 **"왜 그렇게 했는지"**(실측ᆞ
  기각된 가설 포함).
- `docs/dev_log_*.md`는 그 시점의 스냅샷이라 **나중에 고치지 않는다**.

## 개발 환경 세팅 (새 컴퓨터에서 시작할 때)

`.env`ᆞ`chroma_db/`ᆞ`.venv/`는 **git에 없다.**

```bash
uv sync --extra dev
cp .env.example .env                      # GEMINI_API_KEY 필수, 나머지는 선택
uv run python -m src.pipeline --ingest     # ← 필수! 빠뜨리면 검색 0건
uv run pytest                              # 200 passed 나와야 정상
uv run uvicorn src.api:app --reload        # http://localhost:8000/ui
```

**인덱싱을 빠뜨리면 에러 없이 조용히 틀린 답이 나간다** — 검색이 실패하는 게 아니라
"가장 비슷한" 무관한 조문을 돌려주기 때문이다(2026-08-02에 법령 6종이 누락된 채
운영되고 있었다). 서버 시작 시 미인덱싱 경고가 뜨니 로그를 확인할 것.

첫 실행은 임베딩ᆞ리랭커 모델(약 2.3GB) 다운로드로 몇 분 걸린다.

## 실행 관례

- 파이썬은 `uv run python ...`으로 실행한다. `.venv/bin/python`을 직접 쓰면
  venv의 `bin/`이 PATH에 없어 외부 실행 파일(`hwp5html` 등)을 못 찾는 경우가 있다.
- 테스트는 LLM을 호출하지 않는다(전부 결정론적). API 키 없이도 돌아가야 한다.
